<UserControl x:Class="TimeTracker.Views.TabTaskListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TimeTracker.Views"
             xmlns:dataTypes="clr-namespace:TimeTracker.DataTypes"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type dataTypes:Task}">
            <local:TaskView></local:TaskView>
        </DataTemplate>
    </UserControl.Resources>
    
    <Grid>
        <ListView ItemsSource="{Binding TaskList}"/>
    </Grid>
</UserControl>
